/*
	Navigation.registerComponent(
		'products',
		() => (props) => (
			<Provider store={store}>
				<ScreenWrapper>
					<ProductsView {...props} />
				</ScreenWrapper>
			</Provider>
		),
		() => ProductsView,
	);
*/

// const ProductScreenName = 'AddToCard.ProductList'
// const

// const screenMap = new Map();
// screenMap.set('products', <ProductsView {...props} />)
